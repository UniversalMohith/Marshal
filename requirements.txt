# Microsoft Foundry Agent Service (new projects API, 2.x line)
azure-ai-projects>=2.0.0
azure-identity>=1.19
openai>=1.0
python-dotenv>=1.0

# Foundry IQ grounding (Azure AI Search knowledge bases / agentic retrieval).
# Knowledge bases need the PREVIEW package: pip install --pre azure-search-documents
# Optional — Marshal degrades to local grounding if it is not installed.
azure-search-documents>=11.6.0

# Live UI / API (used in the UI phase)
fastapi>=0.111
uvicorn[standard]>=0.30

# MCP (Model Context Protocol) client — connect external tool servers so Marshal's
# agents can call their tools. Optional: the MCP Settings panel degrades gracefully
# if it is not installed (you get a clear error when adding a server).
mcp>=1.27

# Nicer CLI output for the headless demo
rich>=13.7
